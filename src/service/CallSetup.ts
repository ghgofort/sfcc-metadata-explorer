/**
 * @file CallSetup.ts
 * @fileoverview - A class for the setup of an OCAPI call to an SFCC instance.
 */

export class CallSetup {
  constructor(callName: string, callData: object) {
    /** @todo : Implement to gather the object data from teh callData Object */
  }
}
