/**
 * @file Sort.ts
 * @fileoverview - Exports a single OCAPI document class for a Sort used for
 * sorting searches.
 */

export default class Sort {
  /**
   * @param {Object} [args]
   */
  constructor(args) {
    /** @todo */
  }
}
